import { makeStyles } from '@material-ui/core/styles'

const useStyles = makeStyles({
  stats: {
    display: 'flex',
    alignItems: 'center'
  },
  statsText: {
    marginLeft: 20
  }
})

export default useStyles
