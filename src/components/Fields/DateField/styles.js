import { makeStyles } from '@material-ui/core/styles'

const useStyles = makeStyles({
  container: {
    marginTop: 16,
    marginBottom: 8
  },
  input: {
    width: '100%'
  }
})

export default useStyles
